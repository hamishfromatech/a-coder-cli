# A-Coder Orchestrator (experimental)

> **Package name:** the npm package is still `@earendil-works/pi-orchestrator` — the scope is inherited from the upstream pi project this codebase was forked from. It will move to `@the-atech-corp/...` when npm publishing is enabled.


Experimental. This package is under active development and may change or be removed without notice. Its CLI, APIs, and behavior are not yet stable.

Orchestrator package for pi.

## CLI

```bash
orchestrator --help
```
