[package]
name = "a-coder-desktop"
version = "0.80.92"
description = "Desktop wrapper for a-coder-cli"
authors = ["The A-Tech Corporation PTY LTD"]
edition = "2021"
rust-version = "1.77"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["tray-icon"] }
tauri-plugin-shell = "2"
tauri-plugin-dialog = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = { version = "1", features = ["unbounded_depth"] }
serde_stacker = "0.1"
tokio = { version = "1", features = ["full"] }
uuid = { version = "1", features = ["v4"] }
tracing = "0.1"
walkdir = "2"
which = "7"
base64 = "0.22"
reqwest = { version = "0.13", features = ["json", "multipart"] }

[target.'cfg(target_os = "macos")'.dependencies]
objc = "0.2"

# Linux: direct handle to the WebKitGTK webview for WebProcess-crash recovery
# (must match wry's pinned version + features so the crate unifies).
[target.'cfg(target_os = "linux")'.dependencies]
webkit2gtk = { version = "=2.0.2", features = ["v2_38"] }

[dev-dependencies]
tempfile = "3"

[profile.release]
strip = true
lto = true
opt-level = 3
